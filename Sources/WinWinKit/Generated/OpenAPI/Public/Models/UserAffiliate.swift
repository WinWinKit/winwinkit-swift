//
// UserAffiliate.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** The user&#39;s affiliate standing with this app. */
public struct UserAffiliate: Codable, Hashable {

    public enum Status: String, Codable, CaseIterable {
        case pending = "pending"
        case withdrawn = "withdrawn"
        case approved = "approved"
        case rejected = "rejected"
        case archived = "archived"
        case deactivated = "deactivated"
        case banned = "banned"
    }
    /** The unique identifier of the affiliate. */
    public private(set) var id: String
    /** Where the affiliate stands with this app. `banned` is terminal; `rejected`, `withdrawn`, `archived` and `deactivated` describe affiliates who may apply again. */
    public private(set) var status: Status
    /** The codes the affiliate can currently promote, oldest first. Empty until they create one — an approved affiliate with no code yet is a normal state. */
    public private(set) var codes: [UserAffiliateCode]
    /** The affiliate's totals on this app. */
    public private(set) var stats: UserAffiliateStats

    public init(id: String, status: Status, codes: [UserAffiliateCode], stats: UserAffiliateStats) {
        self.id = id
        self.status = status
        self.codes = codes
        self.stats = stats
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case status
        case codes
        case stats
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(id, forKey: .id)
        try container.encode(status, forKey: .status)
        try container.encode(codes, forKey: .codes)
        try container.encode(stats, forKey: .stats)
    }
}


@available(iOS 13, tvOS 13, watchOS 6, macOS 10.15, *)
extension UserAffiliate: Identifiable {}
